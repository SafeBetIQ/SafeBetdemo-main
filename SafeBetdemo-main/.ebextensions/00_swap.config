commands:
  01_create_swap:
    command: |
      dd if=/dev/zero of=/var/swapfile bs=1M count=2048 && \
      chmod 600 /var/swapfile && \
      mkswap /var/swapfile && \
      swapon /var/swapfile
    test: "[ ! -f /var/swapfile ]"
