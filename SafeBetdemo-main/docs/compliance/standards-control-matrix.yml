# SafeBet — Standards Control Matrix (initial skeleton)
# Architecture Authority v4.0 §15. Machine- + human-readable.
# NOT a certification claim. No copyrighted clause text is reproduced; where a
# clause-level requirement is needed but unavailable, status = STANDARD_TEXT_REQUIRED.
# Editions must be re-verified against the current SABS/ISO source before conformity.

meta:
  version: 0.1-initial
  created_under: SAFEBET_ARCHITECTURE_AUTHORITY v4.0
  certification_claim: NONE
  note: Initial structure only. Controls to be populated by Track E1 with authorised standard text.

controls:
  - control_id: SEC-ISMS-001
    standard: ISO/IEC 27001
    edition: "2022 (verify SA adoption)"
    clause_reference: STANDARD_TEXT_REQUIRED
    product: ALL
    component: shared-security-foundation
    applicability: APPLICABLE
    requirement_summary: "Tenant isolation & access control (defence-in-depth)"
    technical_control: "Postgres RLS on 320/330 tables; RBAC via users.role + casino_id/jurisdiction; principalMayAccessCasino"
    process_control: "Independent review + CI security gate on material changes"
    evidence_required: "RLS coverage report; access-denial logs; review records"
    control_owner: TBD
    validation_method: "Automated RLS test + manual authz review"
    validation_frequency: per-release
    status: PARTIAL
    exceptions: "138 SECURITY DEFINER fns (1 anon-exec = RLS predicate) = tracked debt, A5 in progress (§11.1)"
    review_date: TBD

  - control_id: SEC-PRIVFN-002
    standard: ISO/IEC 27001
    edition: "2022"
    clause_reference: STANDARD_TEXT_REQUIRED
    product: ALL
    component: privileged-db-functions
    applicability: APPLICABLE
    requirement_summary: "No anon/public execution of privileged functions without approved exception (§11.1)"
    technical_control: "EXECUTE grants on SECURITY DEFINER functions"
    process_control: "Track A5 remediation; ADR for any exception"
    evidence_required: "Function ACL inventory"
    control_owner: TBD
    validation_method: "Read-only ACL query + test matrix (anon denied / authorised pass)"
    validation_frequency: per-milestone
    status: "PARTIAL — CONTROLLED EXCEPTION"
    exceptions: "A5 CLOSED (A5.1-A5.5). A5.1 (18 trigger/cron/worker) + A5.2 (12 dormant P0) + A5.3 (31 live-caller-proofed) revoked PUBLIC/anon; A5.4 (3 proven-pure fns) DEFINER->INVOKER; A5.5 final classification of all 138 retained definers (0 UNKNOWN), 138/138 owner postgres, 138/138 pinned search_path, future-regression CI guard (ci:privfn), final baseline + access matrix. Final: SECURITY DEFINER 138, anon 1, public 1, authenticated 84, service_role 137 (secdef-scoped). Sole anon/public = RLS predicate sbiq_may_access_chain_scope (governed allowlist exception, required by RLS execution semantics). NOT CONFORMANT-full because: (1) the RLS-predicate exception is by-design permanent, (2) MFA OPEN P1 hard gate before privileged regulatory-role activation, (3) broad authenticated (84) narrowing deferred to future RBAC/ABAC. No RLS weakening; no new exposure; Production untouched. See PRIVILEGED_FUNCTION_BASELINE.md + FUNCTION_ACCESS_MATRIX.md + ADR-0005."
    review_date: TBD

  - control_id: FIN-FRESH-003
    standard: (SafeBet product rule; QMS-aligned)
    edition: "Authority v4.0 §4.6"
    clause_reference: "Authority §4.6"
    product: SAFEBET_IQ
    component: financial-intelligence
    applicability: APPLICABLE
    requirement_summary: "Freshness contract LOADING|FRESH|STALE|PARTIAL|UNAVAILABLE; no missing/stale -> R0 certified"
    technical_control: "lib/financialFreshness.ts; source_as_of (event time); certifiedMoney null->—"
    process_control: "Unit tests + live verification"
    evidence_required: "financialFreshness.test.mjs; live source_as_of vs snapshot_at delta"
    control_owner: TBD
    validation_method: "node --test + live edge check"
    validation_frequency: per-release
    status: CONFORMANT
    exceptions: none
    review_date: TBD

  - control_id: PRIV-POPIA-004
    standard: ISO/IEC 27701 + POPIA
    edition: "27701:2025"
    clause_reference: STANDARD_TEXT_REQUIRED
    product: ALL
    component: privacy-by-design
    applicability: APPLICABLE
    requirement_summary: "Purpose limitation, minimisation, pseudonymisation, retention, DSR handling"
    technical_control: "player pseudonym tokens; consent_records / data_subject_requests / data_retention_policies tables present"
    process_control: "PIMS not yet formalised"
    evidence_required: "Data-processing register; retention policy enforcement evidence"
    control_owner: TBD
    validation_method: "TBD"
    validation_frequency: TBD
    status: PARTIAL
    exceptions: none
    review_date: TBD

  - control_id: AI-GOV-005
    standard: ISO/IEC 42001
    edition: "2023"
    clause_reference: STANDARD_TEXT_REQUIRED
    product: SAFEBET_IQ
    component: ai-risk-scoring
    applicability: APPLICABLE
    requirement_summary: "AI model inventory, owner, limitations, human oversight, rollback (§13)"
    technical_control: "ai_decision_log table present; bri-risk-score / safeplay-ai-risk-engine edge fns"
    process_control: "No model registry / governance artefacts yet"
    evidence_required: "AI model registry; validation metrics; oversight records"
    control_owner: TBD
    validation_method: "TBD"
    validation_frequency: TBD
    status: PARTIAL
    exceptions: none
    review_date: TBD

  - control_id: BCM-006
    standard: ISO 22301
    edition: "2019"
    clause_reference: STANDARD_TEXT_REQUIRED
    product: ALL
    component: resilience-dr
    applicability: PARTIALLY_APPLICABLE
    requirement_summary: "Defined + tested RTO/RPO per product; restore validation"
    technical_control: "EB rollback versions; Supabase backups"
    process_control: "RTO/RPO undefined"
    evidence_required: "BC/DR plan; restore test evidence"
    control_owner: TBD
    validation_method: "TBD"
    validation_frequency: TBD
    status: GAP
    exceptions: none
    review_date: TBD

  - control_id: BCM-WLI-008
    standard: ISO 22301 + Architecture Authority v4.0 §9
    edition: "22301:2019"
    clause_reference: "Authority §9 (workload isolation)"
    product: SAFEBET_IQ
    component: financial-rollup-worker
    applicability: APPLICABLE
    requirement_summary: "Heavy background processing must not starve auth/OLTP; durable queue + isolated workers"
    technical_control: "EventBridge -> SQS(+DLQ) -> Lambda (reserved concurrency 1) -> rollup RPC; alarms; statement_timeout 120s"
    process_control: "ADR-0002 temporary exception; measured backlog auth guardrail; runbook rollback"
    evidence_required: "auth p95 under backlog; DB lock-wait/connection metrics; DLQ/retry drill"
    control_owner: TBD
    validation_method: "backlog + GoTrue latency measurement (A2.2)"
    validation_frequency: per-change
    status: EXCEPTION_TEMPORARY
    exceptions: "SQL aggregation still executes in Postgres (~30s/refresh). Queue/orchestration ISOLATED; compute NOT. Bounded + low-contention (auth p95 1.59s under 10-msg backlog, 0 lock waits). Exit: Option A (external calc) under separate approval, or optimise refresh. See ADR-0002."
    review_date: "2026-12-03"

  - control_id: SEC-PRODUCT-009
    standard: ISO/IEC 27001 + Architecture Authority v4.0 §7/§10
    edition: "2022"
    clause_reference: "Authority §7 (product boundaries), §10 (shared contracts)"
    product: SAFEBET_GUARDIAN
    component: guardian-standalone-foundation
    applicability: APPLICABLE
    requirement_summary: "New product established with an isolated runtime/API/identity/data boundary; shared services consumed only via governed contracts; no cross-product data leakage; no privileged-execution regression"
    technical_control: "products/guardian package (Option C) + dedicated guardian schema (Option B, RLS on 7 tables, no anon/public, 0 functions); imports limited to @/lib/platform/audit + evidence; MFA hard gate blocks real privileged users; SoD + jurisdiction scoping"
    process_control: "ADR-0006; boundary + independence tests; DB RLS negative tests; ci:privfn guard"
    evidence_required: "boundary/independence tests; DB cross-jurisdiction/cross-product/anon denials; estate unchanged (secdef 138/anon 1/public 1); guardian schema 0 functions"
    control_owner: TBD
    validation_method: "node --test (guardian) + DB RLS negatives + privfn-guard"
    validation_frequency: per-change
    status: "CONFORMANT (C0 foundation; synthetic-only)"
    exceptions: "Foundation only — no business modules, no real integrations, no enforcement. Real privileged Guardian users + production privileged regulatory access BLOCKED pending MFA enforcement (ADR-0005 gate). Data boundary = dedicated schema (interim); separate database/project = final target. See docs/products/guardian/."
    review_date: TBD

  - control_id: SEC-REGISTRY-010
    standard: ISO/IEC 27001 + 27701 + 42001 + Architecture Authority v4.0 §7/§10
    edition: "2022 / 2019(42001)"
    clause_reference: "Authority §7/§10; POPIA minimisation/provenance"
    product: SAFEBET_GUARDIAN
    component: legal-operator-registry
    applicability: APPLICABLE
    requirement_summary: "Authoritative legal/reference model with provenance, effective-dating, history, deterministic matching, jurisdiction isolation, human review of ambiguity, and NO automated illegality determination"
    technical_control: "guardian schema 14 C1 tables (RLS, effective-dating, append-only history, provenance, staging->authoritative); products/guardian/src/registry deterministic matcher + resolveLegalReference (isIllegalDetermination:false); source authority levels + conflict detection + freshness; publication SoD"
    process_control: "ADR-0008; scenario tests 1-7; DB RLS negatives; ci:privfn; MFA hard gate; PROPOSED-NO-LIVE-REGULATOR-INTEGRATION"
    evidence_required: "scenario tests; NO_MATCH!=ILLEGAL invariant test; conflict->human-review; cross-jurisdiction + IQ + anon denials; estate unchanged (secdef 138/anon 1); guardian 0 functions"
    control_owner: TBD
    validation_method: "node --test (guardian registry) + DB RLS negatives + privfn-guard"
    validation_frequency: per-change
    status: "CONFORMANT (C1 foundation; synthetic-only)"
    exceptions: "Synthetic data only; no real regulator/NGB/PLA integration; no domain/app/payment/geo intelligence; no enforcement; no AI legal decision. Runtime serves synthetic snapshot (no DB credential); live DB-read credential path + separate Guardian DB deferred to C2. Real privileged users blocked pending MFA. See docs/products/guardian/."
    review_date: TBD

  - control_id: SEC-DOMAIN-011
    standard: ISO/IEC 27001 + 42001 + 22301 + Architecture Authority v4.0 §7/§10
    edition: "2022 / 2023(42001) / 2019(22301)"
    clause_reference: "Authority §7/§10; queue durability; no automated legal decision"
    product: SAFEBET_GUARDIAN
    component: domain-website-intelligence
    applicability: APPLICABLE
    requirement_summary: "Domain/website intelligence over synthetic subjects: evidence-linked observations, registry comparison via governed contract, explainable review priority, human review of ambiguity, durable queue/DLQ, and NO automated illegality determination or enforcement"
    technical_control: "guardian schema 10 C2 tables (RLS, append-only history, DB CHECK forbidding is_illegal_determination=true); products/guardian/src/domain deterministic normalise+signals+intelligence via resolveLegalReference (isIllegalDetermination:false); SQS+DLQ+worker Lambda (idempotency, ReportBatchItemFailures); no network crawl"
    process_control: "ADR-0009; scenario tests 1-10; DB RLS negatives; live SQS/DLQ test; ci:privfn; MFA hard gate; PROPOSED-NO-LIVE-INTEGRATION"
    evidence_required: "scenario tests; NO_MATCH!=ILLEGAL + illegality-CHECK; mismatch/stale/conflict->review; cross-jurisdiction+IQ+anon denials; idempotency+poison->DLQ live; estate unchanged (secdef 138/anon 1); guardian 0 functions"
    control_owner: TBD
    validation_method: "node --test (guardian domain) + DB RLS negatives + live SQS/DLQ + privfn-guard"
    validation_frequency: per-change
    status: "CONFORMANT (C2 foundation; synthetic-only)"
    exceptions: "Synthetic .test domains only; no real network crawl/DNS; no mobile/payment/geo intelligence; no enforcement; no AI legal decision. Worker/API credential-free; least-privilege DB-write credential path (dedicated guardian-scoped role + Secrets Manager) designed/deferred; separate Guardian DB deferred. Real privileged users blocked pending MFA. See docs/products/guardian/."
    review_date: TBD

  - control_id: SEC-DOMAIN-PERSIST-012
    standard: ISO/IEC 27001 + 22301 + Architecture Authority v4.0 §7/§10/§11
    edition: "2022 / 2019(22301)"
    clause_reference: "Authority §11 (least privilege); secret management; idempotent recovery"
    product: SAFEBET_GUARDIAN
    component: domain-worker-persistence
    applicability: APPLICABLE
    requirement_summary: "Durable domain worker persists to Guardian-owned data via a least-privilege principal, managed secret, bounded repository, idempotent transactional writes, jurisdiction enforcement, and no SafeBet IQ data access"
    technical_control: "dedicated Postgres role guardian_domain_worker (grants ONLY on 10 C2 domain tables + audit_context; no public/IQ grants; RLS enforced via jurisdiction GUC); connection secret in Secrets Manager (worker IAM GetSecretValue on one ARN); DomainObservationRepository (no generic execute); one-transaction idempotent persist (deterministic ids + on-conflict); SQS ReportBatchItemFailures + DLQ + CloudWatch alarm"
    process_control: "ADR-0010; live tests (persist/duplicate/history/wrong-jurisdiction/poison-DLQ/IQ-denial); ci:privfn; MFA hard gate; password rotated + not committed"
    evidence_required: "live persist proof; duplicate suppression (1 authoritative obs); non-destructive history; wrong-jur write blocked by RLS; IQ players SELECT denied; poison->DLQ, 0 partial state; estate unchanged (secdef 138/anon 1); guardian 0 functions"
    control_owner: TBD
    validation_method: "live SQS->worker->DB + DB verification + has_table_privilege checks + privfn-guard"
    validation_frequency: per-change
    status: "PARTIAL - CONTROLLED EXCEPTION (least-privilege on shared Supabase cluster; separate Guardian DB = P1 exit)"
    exceptions: "Interim: guardian_domain_worker connects to the same Supabase cluster as SafeBet IQ (schema + least-privilege principal isolation, NOT a separate cluster). Separate independently-governed Guardian database = P1 exit target. Synthetic only; no real network/crawl; no enforcement; no AI legal decision. MFA gate blocks real privileged users. See docs/products/guardian/ + ADR-0010."
    review_date: TBD

  - control_id: SEC-MOBILE-013
    standard: ISO/IEC 27001 + 42001 + 22301 + Architecture Authority v4.0 §7/§10
    edition: "2022 / 2023(42001) / 2019(22301)"
    clause_reference: "Authority §7/§10; provider neutrality; no automated legal decision"
    product: SAFEBET_GUARDIAN
    component: mobile-app-intelligence
    applicability: APPLICABLE
    requirement_summary: "Provider-neutral mobile app intelligence over synthetic subjects: evidence-linked observations, registry+domain comparison via governed contracts, explainable review priority, human review, durable queue/DLQ, least-privilege persistence, and NO automated illegality determination or enforcement/removal"
    technical_control: "guardian schema 10 C3 mobile_app_* tables (RLS, append-only history, DB CHECK forbidding illegality); products/guardian/src/app deterministic normalise+signals+intelligence via resolveLegalReference + governed app->domain link; SQS+DLQ+app worker Lambda (idempotency, ReportBatchItemFailures); dedicated guardian_app_worker role (no public/IQ; RLS); no platform/network access"
    process_control: "ADR-0011; scenario tests 1-12; DB RLS negatives; live SQS/DLQ + persistence tests; ci:privfn; MFA hard gate; PROVIDER-NEUTRAL + PROPOSED-NO-LIVE-INTEGRATION"
    evidence_required: "scenario tests; NO_MATCH!=ILLEGAL + illegality-CHECK; mismatch/stale/conflict->review; governed app->domain link to real C2 domain; cross-jurisdiction+IQ+anon denials; idempotency+poison->DLQ live; app_worker 0 public grants; estate unchanged (secdef 138/anon 1); guardian 0 functions"
    control_owner: TBD
    validation_method: "node --test (guardian app) + DB RLS negatives + live SQS/DLQ + privfn-guard"
    validation_frequency: per-change
    status: "CONFORMANT (C3 foundation; provider-neutral; synthetic-only)"
    exceptions: "Synthetic app subjects only; no real platform/marketplace/binary/device access; no APP_PLATFORM_REFERRAL; no payment/geo intelligence; no enforcement; no AI legal decision; no provider partnership. Least-privilege persistence on shared Supabase cluster (guardian_app_worker); separate Guardian DB = P1 exit. MFA gate blocks real privileged users. See docs/products/guardian/ + ADR-0011."
    review_date: TBD

  - control_id: SEC-PAYMENT-014
    standard: ISO/IEC 27001 + 27701 + 42001 + 22301 + Architecture Authority v4.0 §7/§10
    edition: "2022 / 2019 / 2023"
    clause_reference: "Authority §7/§10; POPIA minimisation; no automated legal/payment decision"
    product: SAFEBET_GUARDIAN
    component: payment-intelligence
    applicability: APPLICABLE
    requirement_summary: "Provider-neutral, privacy-minimised merchant/payment-channel intelligence over synthetic subjects: registry+domain+app comparison via governed contracts, explainable review, durable queue/DLQ, least-privilege persistence, and NO automated illegality determination, enforcement, or payment action"
    technical_control: "guardian schema 9 C4 payment tables (RLS, append-only history, DB CHECKs forbidding illegality AND enforcement; no PAN/CVV/raw data); products/guardian/src/payment deterministic engine via resolveLegalReference + domain_reference + app_reference contracts; SQS+DLQ+payment worker Lambda; dedicated guardian_payment_worker role (payment tables + audit + contract views only; no public/IQ, no C2/C3 base); new App Reference Contract view"
    process_control: "ADR-0014; scenario tests 1-12; DB RLS negatives; live SQS/DLQ + persistence + governed-link tests; ci:privfn; MFA hard gate; provider-neutral; PROPOSED-NO-LIVE-INTEGRATION"
    evidence_required: "scenario tests; NO_MATCH!=ILLEGAL + illegality/enforcement CHECK; governed domain/app links via views (DOM-SYNTH-0003/APP-SYNTH-0003); cross-jurisdiction+IQ+anon denials; idempotency+poison->DLQ live; payment_worker 0 public/IQ/base-table grants; estate unchanged (secdef 138/anon 1); guardian 0 functions"
    control_owner: TBD
    validation_method: "node --test (guardian payment) + DB RLS negatives + live SQS/DLQ + privfn-guard"
    validation_frequency: per-change
    status: "CONFORMANT (C4 foundation; provider-neutral; synthetic-only)"
    exceptions: "Synthetic payment/merchant only; no real bank/PSP/provider integration; no PAN/CVV/raw customer data; no PAYMENT_REFERRAL/enforcement/provider-response; no geo; no AI legal/payment decision. Least-privilege persistence on shared Supabase cluster; separate Guardian DB = P1 exit. MFA gate blocks real privileged users. See docs/products/guardian/ + ADR-0014."
    review_date: TBD

  - control_id: SEC-GEO-015
    standard: ISO/IEC 27001 + 27701 + 42001 + 22301 + 27017/27018 + Architecture Authority v4.0 §7/§10
    edition: "2022 / 2019 / 2023"
    clause_reference: "Authority §7/§10; POPIA minimisation & purpose limitation; no person-level tracking; no automated legal decision"
    product: SAFEBET_GUARDIAN
    component: geo-jurisdiction-intelligence
    applicability: APPLICABLE
    requirement_summary: "Privacy-preserving property/service/jurisdiction-level geo intelligence over synthetic aggregate-region subjects: registry+domain+app+payment comparison via governed contracts, explainable review, durable queue/DLQ, least-privilege persistence, and NO individual/consumer surveillance, NO ISP/subscriber or bank/cardholder geography, NO automated illegality determination, NO enforcement, NO geo-blocking"
    technical_control: "guardian schema 10 C5 geo tables (RLS, append-only observation+history via trigger guard, DB CHECKs forbidding illegality AND enforcement; NO person entity/column; no ILLEGAL_IN_REGION state); products/guardian/src/geo deterministic engine via resolveLegalReference + domain_reference + app_reference + payment_reference contracts; SQS+DLQ+geo worker Lambda; dedicated guardian_geo_worker role (geo tables + audit + 3 contract views only; no public/IQ, no C2/C3/C4 base); new Payment Reference Contract view; PROHIBITED_PERSON_FIELDS ingest reject-list"
    process_control: "ADR-0015; GUARDIAN_PRIVACY_BOUNDARIES.md; scenario tests 1-14 (incl. person-payload rejection); DB RLS negatives; live SQS/DLQ + persistence + governed-link tests; ci:privfn; MFA hard gate; PROPOSED-NO-LIVE-PROVIDER-INTEGRATION"
    evidence_required: "scenario tests 1-14; NO_MATCH!=ILLEGAL + illegality/enforcement CHECK; governed domain/app/payment links via views (DOM-SYNTH-0003/APP-SYNTH-0003/MER-SYNTH-0001); cross-jurisdiction (ZA-GP vs ZA-WC) + IQ casino_admin + anon denials; idempotency + wrong-jur + poison + person-payload->DLQ live; geo_worker 0 public/IQ/base-table grants; estate unchanged (secdef 138/anon 1); guardian functions = 1 (trigger-only guard, PUBLIC EXECUTE revoked)"
    control_owner: TBD
    validation_method: "node --test (guardian geo) + DB RLS negatives (real role connection) + live SQS/DLQ + privfn-guard"
    validation_frequency: per-change
    status: "CONFORMANT (C5 foundation; privacy-preserving; synthetic-only)"
    exceptions: "Synthetic property/service/aggregate-region only; no individual tracking; no ISP/subscriber browsing history; no bank/cardholder geography; no device/precise-personal-location; no geo/provider enforcement; no AI legal decision. ISO/IEC 27017/27018 clause text: STANDARD_TEXT_REQUIRED. Least-privilege persistence on shared Supabase cluster; separate Guardian DB = P1 exit. MFA gate blocks real privileged users. See docs/products/guardian/ + ADR-0015."
    review_date: TBD

  - control_id: SANS1718-007
    standard: SANS 1718 family
    edition: "verify"
    clause_reference: STANDARD_TEXT_REQUIRED
    product: (machine/device/WRS only)
    component: none-in-current-SafeBet-software
    applicability: NOT_APPLICABLE
    requirement_summary: "Applies to gambling machines/devices/WRS, not SafeBet software"
    technical_control: "N/A unless SafeBet consumes certified machine/WRS data"
    process_control: "Maintain SANS 1718 Applicability Map before any WRS-data consumption"
    evidence_required: "Applicability decision record"
    control_owner: TBD
    validation_method: "N/A"
    validation_frequency: N/A
    status: NOT_APPLICABLE
    exceptions: none
    review_date: TBD
